<?xml version="1.0" encoding="UTF-8"?>
<Datas>

{% for item in zones %}
<zone  serverid="{{ item.serverlist_custom_id }}" ip="{{ item.host_ip_out }}" serverip="{{ item.host_ip_in }}"  name="{{ item.server_group_nickname }}" gameid="{{ item.service_custom_id }}" />
{% endfor %}

</Datas>
